# 投标 Agent MVP-v1.0 试用版优化范围

> 更新时间：2026-05-19  
> 目的：基于外部评估报告和当前 MVP-v1.0 实现状态，明确哪些优化应该继续收口到 1.0，哪些顺延到 v1.1/v1.2。  
> 结论：MVP-v1.0 不继续扩大功能面，而是把“可演示闭环”打磨成“受控试点可用闭环”。

## 1. 总体判断

当前 MVP-v1.0 已经形成业务系统雏形，而不是简单的招标文件问答工具。

已具备的主链路包括：

1. 项目导入和招标文件获取。
2. 文件解析、人工修正和解析版本。
3. 合规矩阵、风险项、来源回链和处理队列。
4. 企业资料库、资料检索和证据绑定。
5. 资格预评估和 Go/No-Go 建议。
6. 商务/资格草稿、事实校验和 Word 导出。
7. 审批任务、提交前核验和审计留痕。

这个阶段适合定位为：

> 面向种子用户的受控试点版本，帮助投标团队少漏项、少翻文件、少编错、少返工；不承诺自动投标、自动写完整技术标或自动提交。

因此，下一步优化不应该继续追求“更像 Agent”，而应该强化“可信试用”：

- 用户知道下一步该处理什么。
- 系统知道哪些风险不能被忽略。
- 每个关键结论能回到来源。
- 审批和导出前有明确阻塞原因。
- 演示样本能稳定跑通并可复盘。

本轮二次完善已锁定三个实施口径：

1. 范围采用 `P0 + 少量 P1`，只补试用可信底线和少量直接降低试用风险的体验项。
2. 提交前核验采用“阻塞 + 可填写风险接受说明继续内部流转”，不做不可绕过的法律级硬拦截。
3. 黄金演示样本新增净化设备/洁净工程脱敏项目，保留原通用样本用于兼容回归。

## 2. 评估报告建议拆解

| 报告建议 | 价值判断 | v1.0 处理方式 | 后续阶段 |
| --- | --- | --- | --- |
| 输入侧 OCR、扫描件、图片表格处理 | 真实项目很重要，但工程量和不确定性较高 | 不进入 v1.0；文档明确“不承诺扫描 PDF” | v1.1/v1.2 根据客户样本决定 |
| 补遗/答疑/附件版本链 | 投标场景高价值，避免文件变更后团队漏跟 | v1.0 做版本过期提醒和手动重生成；完整补遗链顺延 | v1.1 |
| 关键字段不能只依赖 LLM | 截止时间、保证金、格式等错一次风险很高 | v1.0 强化显眼展示和人工确认；复杂规则抽取顺延 | v1.1 |
| 企业资料可信内容运营 | 是产品壁垒，但不宜一次做成完整仪表盘 | v1.0 保留类型、有效期、状态、推荐解释 | v1.1/v1.2 |
| Prompt injection 和外部输入安全 | 必须重视，尤其有网页和文件导入 | v1.0 明确外部内容为非可信输入，不允许自动高权限动作 | v1.1 完善安全策略 |
| 提交准备度检查硬门槛 | 直接影响试用可信度 | v1.0 应继续强化 | v1.0 |
| 证据类型化和证据状态 | 影响资格评估、草稿和审批可信度 | v1.0 做基础字段和推荐解释 | v1.0 + v1.1 |
| 回归评测集和来源一致率 | 是长期质量控制基础 | v1.0 先固定黄金样本和冒烟脚本 | v1.1/v1.2 产品化指标 |
| 多 Agent 编排 | 不是当前最大风险点 | 不进入 v1.0 | v1.1 |
| 净化设备产品选型、图纸、技术标章节 | 意向客户方向正确，但会显著扩大范围 | v1.0 只做资料类型和技术响应入口预埋 | v1.1 |

## 3. MVP-v1.0 应继续完善的范围

### 3.1 提交前核验从“提示”走向“质量闸口”

当前系统已有提交前核验能力，下一步需要继续明确它在工作流里的位置。

建议 v1.0 收口口径：

| 风险 | v1.0 处理 |
| --- | --- |
| 矩阵基于旧解析版本 | 阻塞提交确认；允许用户重新生成矩阵后解除，或填写风险接受说明进入内部复核 |
| 高风险项未确认 | 阻塞提交确认；必须人工确认或填写风险接受说明 |
| 强制项缺企业资料证据 | 阻塞正式审批/导出；允许填写说明后导出内部草稿并留痕 |
| 草稿存在无法验证事实 | 阻塞正式导出；允许填写说明后导出内部草稿并在记录中标记 |
| 资格项待确认 | 警告或阻塞，取决于是否为强制/否决项 |
| 技术响应待确认 | 警告，不阻塞 v1.0 商务/资格草稿 |
| 关键日期缺失 | 警告；需要在项目基础信息中显眼提示 |

1.0 的目标不是做法律级强制拦截，而是让审批人和经办人清楚知道“为什么现在不建议通过”。

### 3.2 版本过期提醒必须稳定、显眼、可操作

完整补遗/答疑版本链可以放到 v1.1，但 v1.0 必须解决最容易踩坑的问题：

> 用户修正了解析结果，但合规矩阵仍基于旧版本。

v1.0 应保持以下能力：

1. 文件解析页显示当前最新解析版本。
2. 合规矩阵页显示矩阵使用版本。
3. 二者不一致时出现明显提醒。
4. 提交前核验中把矩阵过期列为阻塞项。
5. 用户必须显式触发重新生成，不自动覆盖旧矩阵。

这个能力比完整补遗链更小，但对真实试用非常关键。

### 3.3 关键字段需要“可看见、可修改、可确认”

v1.0 不需要把所有关键字段做成复杂规则引擎，但不能让用户不知道系统识别了什么。

建议优先关注：

| 字段 | v1.0 处理 |
| --- | --- |
| 投标截止时间 | 项目头部和提交前核验中显眼展示 |
| 项目名称、标段名称 | 导入预览和工作台中可确认 |
| 预算/最高限价 | 导入预览中展示，后续可作为风险提示 |
| 招标人/代理机构 | 项目基础信息中可维护 |
| 开标时间、保证金、工期、质量标准、格式要求 | v1.0 可先进入矩阵/风险项，v1.1 再结构化字段化 |

判断标准：

- 用户能发现字段缺失。
- 用户能人工修改关键字段。
- 审计中能追踪关键字段变化。

本轮实现范围：

- 项目字段：项目名称、招标人/采购人、代理机构、预算/限价、地区、行业、公告 URL、投标截止时间。
- 标段字段：标段编号、标段名称、预算/限价、投标截止时间。
- 开标时间、保证金、工期、质量标准、格式要求等先继续由合规矩阵承载。

### 3.4 证据推荐解释和资料状态要继续强化

证据绑定是产品信任的核心。v1.0 不追求自动绑定，但必须让用户理解候选证据为什么出现。

建议 v1.0 保持并强化：

1. 推荐原因：说明命中了哪些要求词和资料词。
2. 命中词：展示共同关键词。
3. 资料状态：已确认、草稿、过期、缺证据等。
4. 资料类型：资质、人员、业绩、产品目录、检测报告、技术方案等。
5. 绑定动作：必须由用户人工选择，不自动替用户绑定。

后续 v1.1 可继续扩展：

- 主证据/辅助证据。
- 资料适用地区和行业。
- 资料复核周期。
- 内容健康仪表盘。

### 3.5 草稿事实风险必须在审批和导出前暴露

v1.0 不做复杂逐句批注编辑器，但事实风险不能只藏在后台结果里。

v1.0 应保证：

1. 草稿章节显示事实校验状态。
2. 无法验证事实有明确红色风险提示。
3. 校验失败事实进入提交前核验。
4. Word 导出前展示风险确认。
5. 允许导出内部草稿，但审计和导出记录应标记存在待确认事实。

边界说明：

- v1.0 生成的是商务/资格草稿，不是最终正式标书。
- 无证据内容不得被系统包装成“已确认事实”。
- 最终文本仍由人工审核确认。

### 3.6 审批阻塞原因要成为审批页的一等信息

审批不能只是“通过/退回”按钮。

v1.0 应让审批人看到：

1. 当前是否存在阻塞项。
2. 阻塞项来自版本、风险、证据、事实校验还是审批状态。
3. 创建审批任务时的提交前核验快照。
4. 如果仍然通过，需要填写说明并写入审计。

这能把审批从形式流程变成质量控制。

### 3.7 黄金演示样本和回归脚本要固定

v1.0 是否“可试用”，不能只靠临场演示判断。

建议固定一套黄金演示项目，至少包含：

1. 一份真实或脱敏招标文件。
2. 一组企业资质材料。
3. 一组人员证书。
4. 一组业绩材料。
5. 一组商务模板。
6. 净化方向产品目录、检测报告、技术方案。
7. 故意缺失或过期的材料。
8. 一份导出的合规矩阵 Excel。
9. 一份商务/资格草稿 Word。
10. 一组审批和审计日志记录。

这套样本的目标不是“好看”，而是每次开发后都能跑通端到端回归。

本轮新增样本：

- `洁净车间净化设备采购与安装项目`
- 包含资格要求、强制响应项、技术响应项、评分项和截止时间风险。
- 包含产品目录、检测报告、技术方案、过期资质和草稿承诺材料。
- 故意保留强制项缺证据、高风险待确认和技术响应待确认，用于验证提交前核验。

## 4. MVP-v1.0 不继续扩大的范围

为了避免 1.0 失控，以下能力不进入 1.0 收口：

| 能力 | 原因 | 归属 |
| --- | --- | --- |
| 扫描 PDF OCR、图片表格识别 | 依赖样本质量和 OCR 方案，容易拉长周期 | v1.1/v1.2 |
| 完整补遗/答疑/附件差异管理 | 价值高，但需要新对象和新流程 | v1.1 |
| 完整规则包版本冻结和规则 UI | 应结合垂直行业一起做 | v1.1 |
| 净化设备产品选型 | 需要产品目录、参数 schema 和行业规则 | v1.1 |
| 投标示意图/图纸辅助 | 需要图形生成与专业审核边界 | v1.1 |
| 技术标核心章节生成 | 涉及技术方案和设备选型，不宜提前承诺 | v1.1 |
| 多 Agent 编排 | 当前优先级低于版本、证据、核验和评测 | v1.1 |
| 报价测算 | 高风险业务决策，不进入 1.0 | 后续 |
| 自动报名、自动签章、自动提交 | 涉及账号、CA、法律责任和平台接口 | 不作为近期目标 |
| 复杂 SaaS 租户计费 | 与当前试点目标无关 | 生产化阶段 |

## 5. 建议的 1.0 优化优先级

### P0：试用可信底线

这些能力直接决定种子用户能不能试用。

1. 提交前核验规则和展示继续打磨。
2. 版本过期提醒在文件页、矩阵页、审批/导出前保持一致。
3. 矩阵处理队列默认引导用户先处理 P0/P1 风险。
4. 证据推荐解释、命中词、资料状态展示稳定可用。
5. 草稿事实风险进入审批和导出前提示。
6. 审批任务展示阻塞原因和核验快照。
7. 黄金演示样本和回归脚本固定。

### P1：试用体验增强

这些能力会明显改善业务人员体验，但不应阻塞 1.0 收口。

1. 证据绑定左右工作台，减少弹窗切换。
2. 项目基础信息关键字段编辑和确认状态增强。
3. 解析分块增加搜索和章节定位。
4. 技术响应项可转待办或指派负责人。
5. 流程助手操作日志持久化。
6. 企业资料按类型动态字段。

本轮少量 P1 只实现第 2、3 项：项目/标段关键字段编辑，以及解析分块搜索定位。

### P2：下一阶段准备

这些能力为 v1.1 做铺垫。

1. 补遗/答疑对象模型和版本时间线设计。
2. 净化产品目录 schema。
3. 净化技术参数抽取 schema。
4. 规则包 schema 和版本冻结策略。
5. 多 Agent 输入输出 schema 与审计字段。
6. OCR 和表格识别技术验证。

## 6. MVP-v1.0 成功指标

v1.0 不建议用“生成了多少页标书”来衡量成功，而应看是否降低投标风险和返工。

| 指标 | 关注点 | v1.0 早期目标 |
| --- | --- | --- |
| 必需项召回率 | 资格要求、强制响应项、截止时间、附件要求是否漏掉 | 高风险项宁可多报少漏 |
| 关键字段确认率 | 截止时间、项目名、标段、预算等是否被人工确认 | 关键字段缺失能被发现 |
| 来源覆盖率 | 矩阵项是否能回链招标文件原文 | 关键矩阵项必须有来源 |
| 证据覆盖率 | 资格项和强制项是否绑定企业资料 | 高风险/强制项优先补齐 |
| 事实风险暴露率 | 草稿中无法验证事实是否进入提示 | 审批/导出前必须可见 |
| 阻塞命中率 | 系统拦住的风险是否有业务意义 | 少拦无意义项，多拦真实风险 |
| 单项目首版矩阵耗时 | 从导入到可审阅矩阵需要多久 | 能明显少翻文件 |
| 复盘可解释性 | 能否回答谁在何时依据什么确认了什么 | 审计链路可追踪 |

## 7. 推荐下一轮讨论议题

在不改代码之前，建议先确认以下问题：

1. 提交前核验哪些风险必须阻塞，哪些允许人工说明后继续？
2. v1.0 演示样本使用现有种子数据，还是准备一套更贴近净化设备/洁净工程的脱敏样本？
3. 项目基础信息中，哪些关键字段必须做成显眼可编辑字段？
4. 证据绑定是否需要区分主证据和辅助证据？
5. 审批通过阻塞项时，是否要求填写“风险接受说明”？
6. v1.0 是否需要把“外部文件/网页为非可信输入”的安全边界写入产品说明？
7. v1.1 的第一个垂直行业切片，是先做补遗版本链，还是先做净化设备技术响应/产品目录？

## 8. 建议结论

MVP-v1.0 当前方向正确，下一步不应继续扩功能，而应把以下四件事做稳：

1. 版本：用户清楚知道矩阵是否基于最新解析版本。
2. 证据：用户清楚知道每条关键结论依据什么资料。
3. 核验：系统清楚告诉用户哪些风险会阻塞审批和导出。
4. 回归：团队能用固定样本反复验证闭环质量。

只要这四件事做稳，MVP-v1.0 就可以从“功能完成版”进入“受控试点可用版”。之后再进入 v1.1 做净化设备行业闭环、多 Agent 和更深的版本/规则/安全体系，会更稳。
