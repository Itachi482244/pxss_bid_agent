# 投标 Agent 技术选型文档

> 版本：v0.1  
> 日期：2026-05-14  
> 关联文档：《投标Agent项目架构草案》

## 1. 选型结论

本项目建议采用以 Python 为后端核心、Web B/S 工作台为交互入口、PostgreSQL 为主数据底座、Redis 承载缓存与任务队列、对象存储管理文件资产的技术栈。

项目主要工作负载包括 LLM 调用、文档解析、OCR、RAG 检索、异步任务编排、审批流和审计留痕，整体更偏 IO 密集型和工作流编排型，不属于典型 CPU 密集型系统。因此，早期不建议优先采用 Go、Java、C/C++ 等作为主后端语言。Python 在 AI 生态、文档处理、Agent 编排、规则脚本和快速迭代方面更适合本项目。

推荐原则：

- 主后端优先使用 Python + FastAPI。
- 前端采用 B/S Web 工作台，支持私有化部署和内网访问。
- 长耗时任务必须异步化，避免阻塞 API 请求。
- 数据、文件、模型调用、审批动作和审计日志集中在服务端治理。
- 组件数量先少后多，MVP 阶段避免过早引入复杂中间件。
- 对 LLM、向量库、OCR、对象存储等外部能力保留抽象层，便于后续替换。

## 2. 整体技术栈概览

| 层次 | 推荐技术 | 备选/说明 |
| --- | --- | --- |
| 前端 | React 19/18 + TypeScript + Ant Design | Vue 3 + Element Plus 或 Arco Design 亦可；内部工作台优先考虑组件生态和表格/表单能力 |
| 前端工程 | Vite + React Router | 若需要 SSR、复杂路由或门户能力，可评估 Next.js |
| 后端 API | Python 3.11/3.12 + FastAPI | Python 3.10+ 可兼容；Django 对本项目早期偏重 |
| 异步任务 | Celery + Redis | MVP 可用 ARQ/RQ；生产化更建议 Celery |
| 数据库 | PostgreSQL 16+ 或 15+ | 存储项目、任务、权限、审计、结构化抽取结果 |
| 向量检索 | pgvector + PostgreSQL 全文检索 | 后续可接 Elasticsearch、Qdrant、Milvus |
| 对象存储 | MinIO | 云上可选阿里云 OSS、腾讯云 COS、华为云 OBS |
| 缓存/消息 | Redis 7+ | 会话、缓存、限流、任务队列、短期状态 |
| LLM 接入 | LLM Provider 抽象层 + 国内主流模型 | 通义千问、智谱、DeepSeek、火山方舟等；GPT 可作为备选但需评估数据合规 |
| 文档解析 | PyMuPDF + pdfplumber + PaddleOCR + python-docx + openpyxl | 按文件类型和质量分流处理 |
| 规则引擎 | Python 规则模块 + JSON/YAML 规则配置 | 可评估 rule_engine；早期不建议引入 Drools |
| 审批流 | 数据库状态机 + 服务端审批逻辑 | 后续流程复杂再评估 pgflow、Temporal、Camunda |
| 日志 | structlog + 标准日志采集 | 运行日志与审计日志分离 |
| 审计 | PostgreSQL 审计表 + 文件归档 | 关键动作、模型调用、证据链、人工修改必须留痕 |
| 部署 | Docker Compose 起步 | 生产可演进到 Kubernetes 或私有化部署平台 |
| 监控 | Prometheus + Grafana + Loki/ELK | MVP 可先保留结构化日志和基础健康检查 |

## 3. 前端选型

### 3.1 推荐方案

建议采用 React + TypeScript + Ant Design 构建内部投标 Web 工作台。

推荐组合：

- React 19/18
- TypeScript
- Vite
- React Router
- Ant Design
- TanStack Query 或 SWR
- Zustand 或 Redux Toolkit

### 3.2 选型理由

- 投标工作台需要大量表格、表单、筛选、上传、审批、状态流转和详情页，Ant Design 的企业后台组件更成熟。
- React 生态对复杂交互、编辑器、文件预览、富文本和表格扩展支持较好。
- TypeScript 有利于约束前后端接口、任务状态、权限点和审计事件结构。
- Vite 适合内部管理系统，工程复杂度低于 Next.js。

### 3.3 前端边界

浏览器端只负责展示、编辑、确认和发起任务，不应承担敏感逻辑。

前端不应：

- 长期缓存受限敏感文件。
- 保存模型 API Key。
- 保存平台账号、CA/UKey、签章密钥等高权限凭据。
- 绕过后端直接访问对象存储原始文件。
- 在本地做不可审计的最终文件拼装和提交动作。

## 4. 后端 API 选型

### 4.1 推荐方案

建议采用 Python 3.11/3.12 + FastAPI。

推荐组合：

- FastAPI
- Pydantic
- SQLAlchemy 2.x
- Alembic
- asyncpg 或 psycopg
- httpx
- structlog

### 4.2 选型理由

- Python 是当前 AI 应用、RAG、OCR、文档处理和 Agent 编排生态最完整的语言之一。
- FastAPI 原生支持异步接口，适合处理大量外部模型调用、文件服务和任务状态查询。
- Pydantic 适合定义结构化抽取结果、模型响应、规则校验结果和 API Schema。
- 项目核心不是本地 CPU 密集计算，Python 的性能短板不会成为早期主要瓶颈。

### 4.3 后端设计边界

后端 API 只负责快速请求响应和任务编排入口，长任务必须交给异步 Worker。

不建议在同步 API 中执行：

- 大文件 OCR。
- 批量 PDF 解析。
- 批量向量索引构建。
- 长篇标书生成。
- 多轮 Agent 工作流。
- Office/PDF 大文件导出。

## 5. 异步任务选型

### 5.1 推荐方案

生产化建议采用 Celery + Redis。

MVP 阶段如团队希望更轻量，可采用 ARQ 或 RQ，但需要提前确认后续迁移成本。

### 5.2 适合异步化的任务

- 招标文件解析。
- 扫描件 OCR。
- 文档切分和结构化抽取。
- 向量索引构建。
- 合规矩阵生成。
- 标书章节草稿生成。
- Word/Excel/PDF 导出。
- 批量证据回链。
- 官方公告抓取和定时同步。

### 5.3 任务治理要求

每个异步任务应记录：

- 任务 ID。
- 项目 ID、文档 ID、标段 ID。
- 创建人和触发来源。
- 当前状态。
- 重试次数。
- 输入参数摘要。
- 产物文件 ID。
- 错误原因。
- 开始和结束时间。

## 6. 数据库与检索选型

### 6.1 PostgreSQL

建议以 PostgreSQL 作为主数据库，优先选择 PostgreSQL 16+，如企业环境已有 PostgreSQL 15，也可以兼容。

适合存储：

- 用户、组织、角色、权限。
- 项目、标段、任务、审批。
- 文档元数据。
- 条款抽取结果。
- 合规矩阵。
- 审计日志。
- 模型调用记录。
- 文件版本和导出记录。

### 6.2 pgvector

MVP 阶段建议使用 pgvector 承载轻量向量检索，减少基础设施复杂度。

适合场景：

- 企业资料检索。
- 历史标书片段检索。
- 招标条款相似项检索。
- 产品方案片段召回。
- 法规和模板片段召回。

### 6.3 混合检索

RAG 不应只依赖向量相似度，建议采用混合检索：

- PostgreSQL 全文检索或关键词检索。
- pgvector 向量检索。
- 元数据过滤。
- 项目、客户、行业、标段、文档类型权限过滤。
- 重排序。
- 引用片段和来源页码返回。

后续当数据规模扩大或检索复杂度提升时，再评估 Elasticsearch、Qdrant 或 Milvus。

## 7. 文件与对象存储选型

### 7.1 推荐方案

私有化部署优先使用 MinIO。云环境可选择阿里云 OSS、腾讯云 COS 或华为云 OBS。

### 7.2 存储对象

- 招标文件原文。
- 附件和澄清文件。
- OCR 中间产物。
- 切分后的文本片段。
- 生成的标书章节。
- 合规矩阵导出文件。
- Word、Excel、PDF 预览文件。
- 文件版本快照。

### 7.3 安全要求

- 文件必须绑定项目、标段、数据等级和权限策略。
- 前端通过后端授权获取短期访问链接。
- 敏感文件下载、预览、导出必须写入审计日志。
- 受限文件不应直接暴露永久公网地址。
- 删除操作应优先软删除并保留审计记录。

## 8. LLM 接入选型

### 8.1 推荐方案

建设统一 LLM Provider 抽象层，不直接把业务代码绑定到某一个模型厂商。

首批可接入：

- 通义千问。
- 智谱。
- DeepSeek。
- 火山方舟。
- 其他满足企业合规要求的私有化或专有云模型。

GPT 系列模型可作为能力对比或备选，但涉及企业敏感数据、人员信息、报价和未公开投标材料时，必须经过数据合规评估。

### 8.2 抽象层能力

LLM 抽象层应支持：

- 多供应商配置。
- 模型路由。
- 超时和重试。
- Token 用量统计。
- Prompt 模板版本记录。
- 响应结构化解析。
- 敏感数据脱敏。
- 模型调用审计。
- 项目级模型版本冻结。

### 8.3 使用边界

LLM 可以用于理解、归纳、生成和解释，但不应单独决定高风险事项。

以下事项必须结合规则引擎和人工确认：

- 资格真实性。
- 强制响应项。
- 报价和关键商务承诺。
- 投标文件最终提交。
- 电子签章。
- 涉及法律责任的承诺。

## 9. 文档解析选型

### 9.1 推荐组合

| 文件类型 | 推荐工具 |
| --- | --- |
| PDF 文本 | PyMuPDF |
| PDF 表格 | pdfplumber |
| 扫描件/图片 | PaddleOCR |
| Word | python-docx |
| Excel | openpyxl |
| PDF 预览/转换 | LibreOffice、Poppler 或专用转换服务 |

### 9.2 处理策略

文档解析应按文件质量和文件类型分流：

- 可复制 PDF 优先走文本解析。
- 表格密集 PDF 使用表格解析。
- 扫描件走 OCR。
- Word 保留标题层级、段落、表格和样式信息。
- Excel 保留 Sheet、单元格、公式、合并单元格和表头信息。

解析结果需要保留页码、章节、段落、表格位置和原文片段，便于后续证据回链。

## 10. 规则引擎选型

### 10.1 推荐方案

早期采用 Python 规则模块 + JSON/YAML 规则配置。

规则配置适合描述：

- 资质有效期校验。
- 必填材料清单。
- 强制响应项。
- 文件格式要求。
- 签章盖章要求。
- 报价异常阈值。
- 截止时间提醒。
- 地方平台规则差异。

### 10.2 不建议早期引入重规则引擎

Drools、Camunda 等组件能力强，但早期会增加部署、学习、建模和运维复杂度。本项目第一阶段更重要的是把规则数据、证据链、人工确认和审计闭环打通。

当规则规模、流程复杂度或跨部门建模需求明显提升后，再评估独立规则服务或流程引擎。

## 11. 审批流选型

### 11.1 推荐方案

MVP 阶段采用简单状态机 + 数据库实现审批流。

典型状态：

- 待解析。
- 解析中。
- 待确认。
- 待补充。
- 已确认。
- 已驳回。
- 已导出。
- 已归档。

### 11.2 审批要求

审批流必须支持：

- 角色权限控制。
- 当前责任人。
- 审批意见。
- 驳回原因。
- 人工修改差异记录。
- 关键动作双人复核。
- 审计日志不可篡改。

## 12. 日志与审计选型

### 12.1 运行日志

运行日志用于排查系统问题，建议使用 structlog 输出结构化日志。

记录内容包括：

- 请求 ID。
- 用户 ID。
- 项目 ID。
- 接口路径。
- 任务 ID。
- 错误堆栈。
- 外部服务耗时。

### 12.2 审计日志

审计日志用于合规追溯，应与普通运行日志分开设计。

审计日志记录：

- 文件上传、预览、下载、删除。
- 文档解析。
- RAG 检索证据。
- 模型调用。
- 规则校验。
- 章节生成。
- 人工修改。
- 审批确认。
- 导出文件。
- 权限变更。

关键审计记录应写入 PostgreSQL 审计表，并可定期归档到对象存储。

## 13. 部署建议

### 13.1 MVP 部署

MVP 阶段建议使用 Docker Compose：

- frontend
- backend-api
- worker
- PostgreSQL
- Redis
- MinIO

这种方式部署简单，适合演示、验证和小规模内部试用。

### 13.2 生产部署

生产阶段可演进为：

- Kubernetes 或企业私有化部署平台。
- 前后端分离部署。
- Worker 独立扩缩容。
- PostgreSQL 主备和备份。
- Redis 高可用。
- MinIO 集群或云对象存储。
- 集中日志和监控。

## 14. 技术演进边界

### 14.1 暂不优先引入

MVP 阶段不建议优先引入：

- 微服务拆分。
- Kubernetes 强依赖。
- Camunda 等重流程引擎。
- Drools 等重规则引擎。
- 独立大型向量数据库。
- 全量 MLOps 平台。
- 自动电子签章和自动提交。

### 14.2 后续可替换或增强

当业务规模提升后，可逐步增强：

- pgvector 替换或扩展为 Qdrant、Milvus、Elasticsearch 混合检索。
- Celery Worker 按任务类型拆分队列。
- OCR 独立为专用服务。
- LLM 调用增加模型网关。
- 审批流升级为流程引擎。
- 高吞吐连接器用 Go 独立实现。
- 报表和审计查询接入 BI 工具。

## 15. 推荐落地顺序

第一阶段建议按以下顺序建设：

1. 建立 FastAPI 后端、PostgreSQL、Redis、MinIO 基础工程。
2. 建立 React + TypeScript + Ant Design Web 工作台。
3. 实现用户、项目、文件上传、对象存储和权限控制。
4. 实现文档解析异步任务。
5. 实现条款抽取和合规矩阵数据模型。
6. 接入 LLM Provider 抽象层。
7. 实现 pgvector 知识库检索。
8. 实现章节草稿生成和证据引用。
9. 实现审批状态机和审计日志。
10. 实现 Word/Excel 导出。

## 16. 最终建议

本项目的技术栈应以“AI 能力集成效率、文档处理能力、数据安全治理、审批审计闭环”为核心，而不是单纯追求极限并发或底层性能。

因此推荐技术路线为：

```text
React + TypeScript + Ant Design
Python + FastAPI
Celery + Redis
PostgreSQL + pgvector
MinIO
LLM Provider 抽象层
PyMuPDF/pdfplumber/PaddleOCR/python-docx/openpyxl
Python 规则模块 + JSON/YAML 配置
数据库状态机审批流
structlog + PostgreSQL 审计表
```

该组合足以支撑 MVP 和早期生产化。后续只需要在明确瓶颈出现后，对 OCR、检索、流程、连接器或模型网关做局部服务化和专项增强。
