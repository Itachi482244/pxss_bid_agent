# 投标 Agent 质量属性分析

> 版本：v0.1  
> 日期：2026-05-14  
> 范围：以已冻结的 MVP-v0.5 为主，同时识别 SaaS 化、MVP-v1.0 和生产化阶段的架构风险。

## 1. 分析目标

本项目的主要架构风险不只来自功能复杂度，还来自数据安全、审计追溯、AI 生成可信度、文档解析可靠性、地方规则差异和 SaaS 多租户隔离。

本质量属性分析用于回答：

- MVP-v0.5 至少要满足哪些质量门槛。
- 哪些质量属性会驱动架构设计。
- 哪些质量属性可以后置到 MVP-v1.0 或生产化。
- 哪些风险必须通过架构机制而不是业务约定解决。

## 2. 需求冻结前提

MVP-v0.5 范围已冻结，核心闭环为：

1. 手工创建项目和标段。
2. 上传招标文件，或通过公开资源站点链接获取招标文件附件。
3. 记录招标文件来源，包括本地上传、公开链接、资源站点名称、原始 URL、获取时间。
4. 基础文档解析，优先支持可复制 PDF 和 Word。
5. 抽取资格要求和强制响应项。
6. 生成基础合规矩阵。
7. 矩阵支持人工确认、人工修改、责任人标记和风险标记。
8. 高风险项必须逐条确认。
9. 导出 Excel 合规矩阵。
10. 基础审计日志。

MVP-v0.5 不包含：

- 自动公告批量抓取和机会订阅。
- 复杂资源站点登录、验证码、自动报名、CA/UKey、付费下载和非公开附件获取。
- 企业知识库和 RAG。
- 标书章节生成。
- 完整审批流。
- 报价辅助。
- 复杂地方规则包管理。
- 智能助手执行类动作。

## 3. 关键质量属性

| 质量属性 | 重要性 | 风险 | 说明 |
| --- | --- | --- | --- |
| 安全性 | 高 | 高 | 涉及招标文件、企业资料、人员材料、报价和项目敏感信息 |
| 租户隔离 | 高 | 高 | SaaS 模式下必须防止跨租户、跨项目数据泄露 |
| 审计性 | 高 | 高 | 投标业务要求全程留痕，关键修改必须可追溯 |
| 可追溯性 | 高 | 高 | 合规矩阵和结论必须回链文件来源和页码 |
| 可靠性 | 高 | 中 | 文档解析、下载、导出属于长任务或易失败任务 |
| 可修改性 | 高 | 中 | 后续需替换模型、OCR、规则包、检索能力和对象存储 |
| 性能 | 中 | 中 | 矩阵高密度操作和文件解析任务需要可接受响应 |
| 可用性 | 中 | 中 | 外部资源站点、模型、OCR、Worker 失败时需降级和可恢复 |
| AI 可信度 | 高 | 高 | MVP-v0.5 主要抽取矩阵；后续生成章节必须防幻觉 |
| 可运维性 | 中 | 中 | 需要任务状态、日志、重试、健康检查和故障定位 |

## 4. 质量属性场景

### 4.1 安全性

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-SEC-001 | 用户访问项目文件 | 用户请求查看招标文件或解析结果 | 系统验证租户、项目 ACL、角色和数据等级后返回授权内容 | 未授权访问拦截率 100% |
| QA-SEC-002 | SaaS 查询隔离 | 用户查询项目列表、矩阵、文件或任务 | 查询条件自动注入租户和授权项目范围 | 不允许出现跨租户数据 |
| QA-SEC-003 | 文件下载控制 | 用户下载原始招标文件或导出文件 | 后端生成短期链接并记录审计日志 | 文件链接具备有效期，下载动作可追溯 |
| QA-SEC-004 | 外部资源站点下载 | 用户输入公开招标文件链接 | 系统校验 URL、记录来源、限制下载大小和文件类型 | 阻断非允许协议和超限文件 |

架构策略：

- 安全入口网关统一处理 TLS、限流、租户识别和请求审计。
- 后端所有查询、文件访问、RAG 检索、模型上下文和工具调用先经过安全策略组件。
- 对象存储不暴露永久公网地址，只通过后端授权短期链接访问。
- 文件获取限制协议、域名、大小、文件类型和超时时间，防止 SSRF 和资源滥用。

### 4.2 审计性

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-AUD-001 | 修改合规矩阵 | 用户修改矩阵项内容、状态或责任人 | 系统要求填写修改原因，记录修改前后差异 | 关键修改审计覆盖率 100% |
| QA-AUD-002 | 修正文档解析结果 | 用户人工修正解析结果 | 系统记录原因、差异、影响范围，并触发矩阵重生成或增量更新 | 修正链路可回放 |
| QA-AUD-003 | 导出 Excel | 用户导出合规矩阵 | 系统二次确认，记录导出人、时间、筛选条件、文件版本 | 导出记录可追溯 |
| QA-AUD-004 | 从资源站点获取文件 | 用户通过链接获取招标文件 | 系统记录 URL、资源站点名称、获取时间、文件哈希和操作者 | 文件来源可追溯 |

架构策略：

- 审计日志作为独立业务能力设计，不混同普通运行日志。
- 审计事件结构化，至少包含 actor、tenant、project、section、object、action、before、after、reason、timestamp。
- 文件版本和导出文件记录哈希，便于后续比对。

### 4.3 可追溯性

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-TRACE-001 | 查看矩阵项来源 | 用户点击证据来源 | 系统展示原文页码、段落、来源文件和获取方式 | 矩阵项来源覆盖率 >= 95% |
| QA-TRACE-002 | 解析版本冻结 | 用户查看已冻结解析结果 | 系统展示版本号、生成时间、来源文件、修正记录 | 版本链完整 |
| QA-TRACE-003 | 增量更新矩阵 | 解析结果被修正 | 系统标记受影响矩阵项，并记录更新来源 | 受影响项可定位 |

架构策略：

- 文档解析产物保留页码、段落、表格位置、原文片段和 chunk ID。
- 合规矩阵项保存 source_document_id、source_version_id、page_no、chunk_id。
- 解析版本、矩阵版本、导出版本建立关联。

### 4.4 可靠性

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-REL-001 | 文档解析失败 | PDF 解析失败或格式异常 | 任务进入失败状态，记录错误，允许重试或人工处理 | 失败原因可见，重试不产生重复矩阵 |
| QA-REL-002 | 资源站点下载失败 | 链接超时、404、文件过大或类型不支持 | 系统给出失败原因，不创建脏文件记录 | 失败任务可重试 |
| QA-REL-003 | Worker 重启 | 解析任务执行中 Worker 重启 | 任务可恢复或重新入队，不丢失状态 | 任务状态一致 |
| QA-REL-004 | 导出失败 | Excel 导出失败 | 系统保留错误原因，可重新导出 | 不产生半成品正式文件 |

架构策略：

- 长任务异步化，任务状态持久化。
- 任务具备幂等键，避免重复解析和重复生成矩阵。
- 下载、解析、导出分别设置超时、重试和失败状态。

### 4.5 性能

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-PERF-001 | 打开 100 行合规矩阵 | 用户进入矩阵页 | 矩阵加载完成 | < 2 秒 |
| QA-PERF-002 | 筛选 100 行矩阵 | 用户按状态/风险/责任人筛选 | 表格刷新 | < 2 秒 |
| QA-PERF-003 | 下载公开招标文件 | 用户输入公开附件链接 | 下载任务创建并反馈状态 | API 响应 < 1 秒，下载后台执行 |
| QA-PERF-004 | 文档解析 | 用户上传或下载招标文件后解析 | 用户可查看任务进度 | 长任务异步执行，不阻塞页面 |

架构策略：

- 前端矩阵分页或虚拟滚动预留。
- API 只创建任务和返回状态，不同步等待下载、OCR 或解析。
- 解析结果和矩阵查询加索引，按 project_id、section_id、status、risk、owner 过滤。

### 4.6 可修改性

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-MOD-001 | 替换 OCR 能力 | OCR 工具从本地换成云 OCR | 只修改适配器，不影响业务服务 | 修改范围局限于工具层 |
| QA-MOD-002 | 增加地方规则包 | 新增省市或平台规则 | 通过规则配置和版本发布生效 | 不重启核心服务或少量配置刷新 |
| QA-MOD-003 | 替换对象存储 | MinIO 换成 OSS/COS | 只替换存储适配器 | API 契约不变 |
| QA-MOD-004 | 后续接入 RAG | MVP-v1.0 增加企业知识库检索 | 新增检索服务，不重写矩阵核心 | 合规矩阵数据模型可复用 |

架构策略：

- LLM、OCR、对象存储、资源站点下载、规则引擎都通过适配器封装。
- 规则配置支持热加载或版本刷新。
- 业务服务依赖稳定接口，不直接依赖具体供应商 SDK。

### 4.7 AI 可信度

| 场景编号 | 场景 | 刺激 | 响应 | 度量 |
| --- | --- | --- | --- | --- |
| QA-AI-001 | 抽取资格要求 | 模型或规则抽取招标文件资格项 | 输出必须绑定原文来源和页码 | 无来源结论不得自动确认 |
| QA-AI-002 | 模型输出不确定 | 抽取结果置信度低或来源冲突 | 标记为待人工确认 | 高风险项不得批量确认 |
| QA-AI-003 | 后续章节生成 | 模型生成章节草稿 | 自动事实性校验，无法验证内容标注人工确认 | 无证据事实不得进入正式版本 |

架构策略：

- MVP-v0.5 中 LLM 只用于辅助抽取，关键项必须回链原文。
- 后续章节生成必须引入事实性校验和无法验证占位机制。
- 高风险项、强制项、缺材料项不得批量确认。

## 5. MVP-v0.5 质量门槛

MVP-v0.5 验收时至少满足：

- 所有项目、标段、文件、矩阵项必须带租户/项目范围字段。
- 文件上传和公开链接获取均记录来源、时间、操作者和文件哈希。
- 文档解析和导出均为异步任务或可恢复任务。
- 合规矩阵项必须保留来源文件和页码/片段信息。
- 人工修改矩阵项必须填写修改原因。
- 高风险项、强制项、缺材料项不得批量确认。
- 100 行矩阵加载和筛选响应时间小于 2 秒。
- API 层不得直接暴露对象存储永久地址。
- 基础审计日志覆盖文件、解析、矩阵修改、确认、导出。

## 6. 主要风险与缓解策略

| 风险 | 影响 | 缓解策略 |
| --- | --- | --- |
| 公开资源站点格式不稳定 | 文件获取失败或解析失败 | v0.5 只支持手工链接和公开附件，失败可人工上传 |
| SSRF 或恶意链接 | 安全风险 | 限制协议、域名、大小、Content-Type、超时和重定向 |
| 文档解析质量不稳定 | 矩阵错误或漏项 | 保留人工修正、版本管理和重新生成矩阵机制 |
| AI 抽取幻觉 | 错误合规项 | 结论必须回链原文，低置信度进入人工确认 |
| 审计遗漏 | 无法追责 | 审计作为独立服务能力，关键动作统一封装 |
| MVP 范围膨胀 | 延期 | v0.5 范围冻结，新增需求进入 v1.0 或二期 |

## 7. 后续 ADR 建议

建议基于本质量属性分析生成以下 ADR：

- ADR-001：采用 B/S 架构与服务端集中治理。
- ADR-002：采用 Python + FastAPI 作为主后端。
- ADR-003：采用异步任务处理文档解析、下载和导出。
- ADR-004：采用对象存储保存原始文件和导出产物。
- ADR-005：采用安全入口网关和策略组件实现 SaaS 租户隔离。
- ADR-006：合规矩阵必须保存证据回链。
- ADR-007：MVP-v0.5 不做复杂资源站点登录和自动报名。
