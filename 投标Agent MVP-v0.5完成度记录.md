# 投标 Agent MVP-v0.5 完成度记录

> 更新时间：2026-05-15  
> 范围基线：`投标Agent需求清单.md` 第 21.1 节  
> 当前判断：MVP-v0.5 冻结范围已完成，当前系统已具备“建项目 → 获取/上传文件 → 解析 → 生成矩阵 → 人工处理 → 导出矩阵快照 → 审计留痕”的最小闭环。

## 1. 状态定义

| 状态 | 含义 |
| --- | --- |
| ✅ 已完成 | 已有后端能力、前端入口或可验证测试，能按 MVP 口径使用 |
| 部分完成 | 核心数据结构或局部能力已实现，但缺 UI、接口、审计、测试或文件类型覆盖 |
| 未开始 | 尚未形成可用实现 |

## 2. 总体进度

按 MVP-v0.5 的 10 个冻结范围项统计：

| 状态 | 数量 | 说明 |
| --- | ---: | --- |
| ✅ 已完成 | 10 | MVP-v0.5 冻结范围均已完成 |
| 部分完成 | 0 | 无 |
| 未开始 | 0 | 无 |

当前已跑通的最短链路：

1. 使用真实 Word 招标文件建立演示项目。
2. 浏览器中新建项目和默认标段。
3. 上传 Word/PDF 或填写公开附件链接获取招标文件。
4. 文档进入解析任务，Word 保留标题/段落/表格，PDF 保留页码和文本片段。
5. 从分块中抽取资格要求、强制响应项、评分办法、格式要求、截止时间和参考信息。
6. 生成合规矩阵并在前端工作台展示。
7. 支持逐条确认、编辑矩阵项、调整风险/状态、指派责任人，并记录审计日志。
8. 高风险、强制项、缺材料或无来源项禁止批量确认。
9. 导出 Excel 格式的合规矩阵快照，保存文件哈希、导出记录和下载审计；导出文件仅作为归档/协作快照，最新状态以平台为准。
10. 记录文档上传、公开链接、解析、矩阵生成、矩阵人工处理和导出下载的基础审计日志。

## 3. MVP-v0.5 范围项完成度

| 序号 | MVP-v0.5 范围项 | 完成标记 | 当前状态 | 已实现内容 | 缺口 |
| ---: | --- | --- | --- | --- | --- |
| 1 | 手工创建项目和标段 | ✅ | 已完成 | 已支持项目创建、默认标段创建、单独创建标段；前端提供新建项目表单 | 后续可补更完整的成员与角色配置 |
| 2 | 上传招标文件，或通过公开资源站点链接获取招标文件附件 | ✅ | 已完成 | 后端支持文件上传和手工公开 URL 获取；前端支持上传 Word/PDF、填写公开附件链接；包含 URL 安全校验、大小限制、重定向校验、MinIO 存储 | 后续可补复杂资源站点登录、验证码等二期能力 |
| 3 | 记录招标文件来源，包括本地上传、公开链接、资源站点名称、原始 URL、获取时间 | ✅ | 已完成 | `documents`、`document_versions`、`file_acquisition_tasks` 已记录来源、URL、站点、获取时间、哈希、对象存储路径；关键动作写入审计 | 后续可补文件下载短期授权链接和下载审计 |
| 4 | 基础文档解析，优先支持可复制 PDF 和 Word | ✅ | 已完成 | Word `.docx` 解析已支持标题、段落、表格和分块入库；PDF 文本解析已支持页码和文本片段；任务状态已落库并在前端展示解析状态 | 后续可补 OCR、PDF 表格结构化和解析结果人工修正版本 |
| 5 | 抽取资格要求和强制响应项 | ✅ | 已完成 | 已有规则抽取和 DeepSeek 调用兜底结构；当前可抽取资格、强制响应、评分办法、格式、截止时间、参考信息；真实文档已生成 33 条矩阵项 | 后续继续提升复杂表格场景和地方规则包命中解释 |
| 6 | 生成基础合规矩阵 | ✅ | 已完成 | 后端任务、幂等键、矩阵项入库、前端矩阵展示、横向滚动和筛选已实现；本地环境支持 inline 执行以完成演示闭环 | 后续可补更细的后台任务队列监控 |
| 7 | 矩阵支持人工确认、人工修改、责任人标记和风险标记 | ✅ | 已完成 | 已支持单条确认、矩阵项编辑、状态/风险/强制项调整、单条/批量指派责任人；修改原因必填；前端按钮已接入真实 API；审计记录 before/after | 后续可补更细的权限角色和责任人选择器 |
| 8 | 高风险项必须逐条确认 | ✅ | 已完成 | 高风险、强制项、缺材料或无来源项禁止批量确认；单条确认写入确认人、确认时间和审计日志 | 后续可补审批人角色限制 |
| 9 | 导出合规矩阵快照 | ✅ | 已完成 | 已支持 Excel 快照导出任务、`.xlsx` 生成、MinIO 保存、文件哈希、导出记录、前端下载入口；文件内明确标注“只读快照，最新状态以平台为准” | 后续可补按筛选条件展示导出历史和更精细的模板样式 |
| 10 | 基础审计日志 | ✅ | 已完成 | 已记录种子数据、项目创建、文件上传、公开 URL 请求/阻断/下载、解析成功/失败、矩阵生成请求/结果、矩阵确认/修改/指派 before/after、导出和下载 | 后续可补审计报表导出和不可变归档策略 |

## 4. 当前代码锚点

| 能力 | 主要位置 |
| --- | --- |
| 项目、标段、矩阵查询 API | `backend/app/api/v1/routes/projects.py` |
| 文档上传、公开 URL 获取、解析任务 API | `backend/app/api/v1/routes/documents.py` |
| Word 解析 | `backend/app/parsers/word.py`、`backend/app/services/document_parse.py` |
| 公开文件获取 | `backend/app/services/file_acquisition.py` |
| 合规矩阵生成 | `backend/app/services/compliance_generation.py` |
| 异步任务 | `backend/app/worker.py` |
| 数据模型 | `backend/app/models/`、`backend/migrations/versions/` |
| 前端首页和项目工作台 | `frontend/src/pages/App.tsx`、`frontend/src/pages/app.css` |
| 当前测试覆盖 | `backend/tests/` |

## 5. 建议下一步

### 5.1 已完成：补齐合规矩阵人工闭环

这一步已经完成，系统从“能看矩阵”推进到“能处理矩阵”。

已实现：

1. 单条确认接口：确认时必须有来源证据，写入 `confirmed_by`、`confirmed_at` 和审计日志。
2. 矩阵项修改接口：支持修改响应建议、状态、风险、强制项和责任人，必须填写修改原因，记录 before/after。
3. 责任人指派接口：单条和批量均支持，批量操作写审计。
4. 批量确认接口：只允许低风险、非强制、非缺材料且有来源证据的矩阵项。
5. 前端确认、编辑、指派、批量确认、批量指派已接入真实 API。

### 5.2 已完成：实现矩阵快照导出

状态：已完成。

已实现：

1. 新增 `excel_export` 任务。
2. 支持按当前筛选条件导出矩阵快照。
3. 生成 `.xlsx`，保存 MinIO，记录文件哈希和导出审计。
4. 前端提供“导出快照”入口和归档快照下载入口。
5. 导出文件内明确标注只读快照属性，最新状态、确认记录和责任分工以平台为准。

### 5.3 已完成：补齐浏览器端最短操作链路

状态：已完成。

已实现：

1. 新建项目和标段表单。
2. 上传文件和公开 URL 获取入口。
3. 文档解析状态展示。
4. 解析失败原因可通过任务错误信息和审计日志追溯。

### 5.4 已完成：实现 PDF 文本解析

状态：已完成。

已实现：

1. 支持可复制 PDF 文本解析，保留页码和原文片段。
2. PDF 表格和 OCR 暂时后置，但接口和任务状态保留扩展点。
3. 已增加 PDF 解析测试。

## 6. 当前验收口径

当前 MVP-v0.5 已满足以下验收口径：

1. 用户能在浏览器中新建项目和标段。
2. 用户能上传 Word 或 PDF，或填写公开附件链接。
3. 用户能看到文件获取、解析、矩阵生成任务状态。
4. 系统能生成合规矩阵，矩阵可筛选、定位、查看证据。
5. 用户能逐条确认高风险项，能修改矩阵项并填写原因。
6. 用户能按需导出 Excel 格式的合规矩阵快照。
7. 上述关键动作均可在审计日志中查到。

## 7. 已补充扩展项

| 扩展项 | 完成标记 | 当前状态 | 已实现内容 |
| --- | --- | --- | --- |
| 从文件导入新建项目 | ✅ | 已完成 | 上传公告/招标文件后自动抽取项目名称、招标人、代理机构、预算、地区、行业、标段和截止时间，用户确认后创建项目、绑定来源文件、自动解析并生成矩阵 |
| 从网页导入新建项目 | ✅ | 已完成 | 填写公开公告网页或附件 URL 后执行安全校验、下载暂存、抽取项目草稿；确认后创建项目、记录来源 URL、解析网页文本或附件并生成矩阵 |
| 导入前人工确认 | ✅ | 已完成 | 导入结果先进入草稿预览，支持用户修正关键字段后再正式创建项目，避免解析错误污染项目库 |
